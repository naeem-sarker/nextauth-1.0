"use server"

export const getUsers = async () => {
    await fetch("http://localhost:4000/health");

    return;
}

export const getUserById = async () => {
    await fetch("http://localhost:4000/health");

    return;
}